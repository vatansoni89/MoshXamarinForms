<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:helloWorld="clr-namespace:HelloWorld;assembly=HelloWorld"
             xmlns:helloWorld1="clr-namespace:HelloWorld;assembly=HelloWorld"
             x:Class="HelloWorld.ListView">
    <ListView x:Name="lstNames" SeparatorColor="Blue" HasUnevenRows="True">
        <ListView.ItemTemplate>
            <DataTemplate>
                <!--<ImageCell Text="{Binding Name}" Detail="{Binding Status}" ImageSource="{Binding ImageUrl}" TextColor="GreenYellow" DetailColor="Brown" />-->
                <ViewCell>
                    <StackLayout Orientation="Horizontal" Padding="5">
                        <Image  Source="{Binding ImageUrl}" />
                        <StackLayout Orientation="Vertical" HorizontalOptions="StartAndExpand">
                            <Label Text="{Binding Name}" />
                            <Label Text="{Binding Status}" />
                        </StackLayout>
                        <Button Text="Follow" />
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>